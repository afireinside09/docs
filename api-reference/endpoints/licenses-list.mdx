---
title: "List Licenses"
api: "GET /licenses"
description: "List user's licenses with optional filtering"
---

## Request

<ParamField header="Authorization" type="string" required>
  Bearer token from Supabase Auth
</ParamField>

<ParamField query="status" type="string">
  Filter by status: `active`, `cancelled`, `suspended`
</ParamField>

<ParamField query="type" type="string">
  Filter by license type
</ParamField>

<ParamField query="limit" type="number" default="50">
  Maximum number of licenses to return
</ParamField>

<ParamField query="offset" type="number" default="0">
  Number of licenses to skip
</ParamField>

## Response

<ResponseField name="licenses" type="array">
  Array of license objects
</ResponseField>

<ResponseField name="count" type="number">
  Total number of licenses matching filters
</ResponseField>

## License Object

<ResponseField name="id" type="string">
  Unique license identifier (UUID)
</ResponseField>

<ResponseField name="user_id" type="string">
  Owner's user ID
</ResponseField>

<ResponseField name="license_key" type="string">
  The license key for activation
</ResponseField>

<ResponseField name="type" type="string">
  License type/tier
</ResponseField>

<ResponseField name="status" type="string">
  Current status: `active`, `cancelled`, `suspended`
</ResponseField>

<ResponseField name="duration" type="string">
  License duration: `1_month`, `1_year`, `lifetime`
</ResponseField>

<ResponseField name="seat_count" type="number">
  Number of seats included
</ResponseField>

<ResponseField name="stripe_subscription_id" type="string">
  Associated Stripe subscription (nullable)
</ResponseField>

<ResponseField name="expires_at" type="string">
  Expiration date (nullable for lifetime)
</ResponseField>

<ResponseField name="created_at" type="string">
  Creation timestamp
</ResponseField>

## Example

<CodeGroup>
```bash cURL
curl -X GET "https://api.sheetmagic.ai/licenses?status=active&limit=10" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch('https://api.sheetmagic.ai/licenses?status=active', {
  headers: {
    'Authorization': 'Bearer <token>',
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```
</CodeGroup>

## Response Example

```json
{
  "licenses": [
    {
      "id": "123e4567-e89b-12d3-a456-426614174000",
      "user_id": "987fcdeb-51a2-3b4c-d5e6-789012345678",
      "license_key": "SM-XXXX-XXXX-XXXX",
      "type": "pro",
      "status": "active",
      "duration": "1_year",
      "seat_count": 5,
      "stripe_subscription_id": "sub_abc123",
      "expires_at": "2025-12-04T00:00:00Z",
      "created_at": "2024-12-04T00:00:00Z",
      "updated_at": "2024-12-04T00:00:00Z"
    }
  ],
  "count": 1
}
```

## Errors

| Status | Description |
|--------|-------------|
| 401 | Invalid or missing token |
| 500 | Server error |
